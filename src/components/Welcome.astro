<section
  class="relative min-h-screen flex items-center justify-center bg-peach-fog overflow-hidden"
>
  <!-- Soft Glow Overlay -->
  <div
    class="absolute inset-0 bg-gradient-to-br from-white/20 via-transparent to-white/10 pointer-events-none"
  >
  </div>

  <div class="relative z-10 text-center px-6">
    <!-- Logo -->
    <div class="flex justify-center mb-8">
      <div
        class="bg-white/70 backdrop-blur-md p-6 rounded-3xl shadow-card border border-white/40"
      >
        <img
          src="/favicon.svg"
          alt="FlowBoard Logo"
          class="h-16 w-16 object-contain"
        />
      </div>
    </div>

    <!-- Title -->
    <h1 class="text-5xl md:text-6xl font-semibold tracking-tight text-ink-900">
      FlowBoard
    </h1>

    <!-- Gradient Subtitle -->
    <p class="mt-4 text-lg md:text-xl text-ink-600 max-w-xl mx-auto">
      Structured Backlog & Sprint Management for modern development teams.
    </p>

    <!-- CTA Buttons -->
    <div class="mt-10 flex flex-col sm:flex-row gap-4 justify-center">
      <a
        href="/login"
        class="px-8 py-3 rounded-2xl bg-peach-500 text-white font-medium shadow-card hover:bg-peach-600 transition duration-200"
      >
        Get Started
      </a>

      <a
        href="/about"
        class="px-8 py-3 rounded-2xl bg-white/70 backdrop-blur-md border border-white/40 text-ink-800 hover:bg-white transition"
      >
        Learn More
      </a>
    </div>
  </div>
</section>
